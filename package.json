{
  "name": "node-and-redis-app",
  "version": "1.0.0",
  "description": "for learning caching on the backend to reduce external requests or frequently accessed db data",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "winterrdog",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.5.0",
    "express": "^4.18.2",
    "redis": "^4.6.8"
  }
}
